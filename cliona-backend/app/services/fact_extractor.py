"""STUB ONLY — out of scope for v1 (CLAUDE.md §9).

Nothing writes to user_facts in v1. This file is a permanent stub for the
lifetime of v1, not a Phase 1 placeholder to be filled in during a later
phase. It must never be called from any live path.
"""


def extract_facts(*args, **kwargs):
    raise NotImplementedError("Fact Extractor is out of scope for v1 — CLAUDE.md §9")
